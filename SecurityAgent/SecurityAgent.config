set (autostart true)

if(PLUGIN_SECURITYAGENT_STARTUPORDER)
set (startuporder ${PLUGIN_SECURITYAGENT_STARTUPORDER})
endif()

map()
    kv(acl ${PLUGIN_SECURITYAGENT_ACL_FILE_NAME})
    kv(dac ${PLUGIN_SECURITYAGENT_DAC_FOLDER})
end()
ans(configuration)
