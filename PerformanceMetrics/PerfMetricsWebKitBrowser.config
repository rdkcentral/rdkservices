set(autostart ${PLUGIN_PERFORMANCEMETRICS_AUTOSTART})

map()
    if(${PLUGIN_PERFORMANCEMETRICS_WEBKITBROWSER_CLASSNAME})
        kv(classname "WebKitBrowser")
    else()
        kv(callsign "WebKitBrowser")
    endif()
end()
ans(configuration)

