{
  "$schema": "plugin.schema.json",
  "info": {
    "title": "Firebolt Privacy Plugin",
    "callsign": "Privacy",
    "locator": "libWPEFrameworkFireboltPrivacy.so",
    "status": "production",
    "description": [
      "The Firebolt Privacy plugin provides functionality to store user settings either in local or in remote."
    ],
    "version": "1.0"
  },
  "configuration": {
    "type": "object",
    "properties": {
      "configuration": {
        "type": "object",
        "required": [],
        "properties": {
          "devicemanifest": {
            "type": "string",
            "description": "Path to Device manifest file (default: /etc/firebolt-device-manifest.json)"
          }
        }
      }
   }
  },
    "interface": {
        "$ref": "${cppinterfacedir}/IFireboltPrivacy.h"
    }
}
