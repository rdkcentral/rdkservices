autostart = "true"

if boolean("@PLUGIN_MESSENGER_STARTUPORDER@"):
    startuporder = "@PLUGIN_MESSENGER_STARTUPORDER@"

configuration = JSON()

rootobject = JSON()
rootobject.add("mode", "@PLUGIN_MESSENGER_MODE@")
configuration.add("root", rootobject)
