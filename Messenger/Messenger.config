set(autostart ${PLUGIN_MESSENGER_AUTOSTART})

if(PLUGIN_MESSENGER_STARTUPORDER)
set (startuporder ${PLUGIN_MESSENGER_STARTUPORDER})
endif()

map()
    key(root)
    map()
        kv(mode ${PLUGIN_MESSENGER_MODE})
    end()
end()

ans(configuration)
