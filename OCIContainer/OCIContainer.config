set (autostart true)
set (preconditions Platform)
set (callsign "org.rdk.OCIContainer")

if(PLUGIN_OCICONTAINER_STARTUPORDER)
map()
set (startuporder ${PLUGIN_OCICONTAINER_STARTUPORDER})
end()
endif()
