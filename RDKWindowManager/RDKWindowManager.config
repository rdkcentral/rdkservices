set (autostart false)
set (preconditions Platform)
set (callsign "org.rdk.RDKWindowManager")

if(PLUGIN_RDK_WINDOW_MANAGER_STARTUPORDER)
set (startuporder ${PLUGIN_RDK_WINDOW_MANAGER_STARTUPORDER})
endif()
