set (autostart ${PLUGIN_LGISYSTEMSERVICE_AUTOSTART})
set (preconditions Platform)
set (callsign "org.rdk.System")

if(PLUGIN_LGISYSTEMSERVICE_STARTUPORDER)
set (startuporder ${PLUGIN_LGISYSTEMSERVICE_STARTUPORDER})
endif()

