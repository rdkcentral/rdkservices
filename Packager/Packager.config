set(autostart ${PLUGIN_PACKAGER_AUTOSTART})

if(PLUGIN_PACKAGER_STARTUPORDER)
map()
set (startuporder ${PLUGIN_PACKAGER_STARTUPORDER})
end()
endif()

map()
    key(root)
    map()
        kv(mode ${PLUGIN_PACKAGER_MODE})
    end()
end()
ans(configuration)
