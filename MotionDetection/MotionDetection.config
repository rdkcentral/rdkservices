set (autostart false)
set (preconditions Platform)
set (callsign "org.rdk.MotionDetection")

if(PLUGIN_MOTIONDETECTION_STARTUPORDER)
map()
set (startuporder ${PLUGIN_MOTIONDETECTION_STARTUPORDER})
end()
endif()
