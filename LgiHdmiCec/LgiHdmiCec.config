set (autostart ${PLUGIN_LGIHDMICEC_AUTOSTART})
set (preconditions Platform)
set (callsign "com.lgi.rdk.HdmiCec")
map()
    key(root)
    map()
      kv(outofprocess ${PLUGIN_LGIHDMICEC_OUTOFPROCESS})
    end()
end()

ans(configuration)
