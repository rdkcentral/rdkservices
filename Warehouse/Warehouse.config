set (autostart ${PLUGIN_WAREHOUSE_AUTOSTART})
set (preconditions Platform)
set (callsign "org.rdk.Warehouse")

if(PLUGIN_WAREHOUSE_STARTUPORDER)
set (startuporder ${PLUGIN_WAREHOUSE_STARTUPORDER})
endif()
