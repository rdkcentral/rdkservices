set (autostart false)
set (preconditions Platform)
set (callsign "org.rdk.VoiceControl")

if(PLUGIN_VOICECONTROL_STARTUPORDER)
map()
set (startuporder ${PLUGIN_VOICECONTROL_STARTUPORDER})
end()
endif()
