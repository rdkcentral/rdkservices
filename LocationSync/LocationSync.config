set (autostart true)

set (preconditions Network)

map()
    kv(interval 10)
    kv(retries 20)
    kv(source ${PLUGIN_LOCATIONSYNC_URI})
    kv(timezonefile ${PLUGIN_LOCATIONSYNC_TIMEZONEFILE})
end()
ans(configuration)
